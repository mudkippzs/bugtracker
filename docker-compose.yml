services:
  bugtracker:
    build: .
    container_name: bugtracker
    ports:
      - "5177:5177"
    volumes:
      # Persist database
      - ./data:/app/data
      # Mount your Code directory (read-only for discovery)
      - ${CODE_DIR:-/home/dev/Code}:/code:ro
    environment:
      - NODE_ENV=production
      - PORT=5177
      - HOST=0.0.0.0
      - CODE_DIR=/code
    restart: unless-stopped
